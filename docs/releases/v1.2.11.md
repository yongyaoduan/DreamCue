# DreamCue v1.2.11

## Highlights

- Adds a manual sync action on Android and macOS account screens for immediate Firebase pull-and-upload sync.
- Adds Firebase password reset by email on Android and macOS.
- Keeps account recovery feedback visible with clear empty-email, invalid-email, and sent-email states.
- Updates Android and macOS package versions to `1.2.11`.

## Verification

- `gradle -p android :app:testDebugUnitTest :app:compileDebugAndroidTestKotlin` passed.
- `gradle -p android :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.dreamcue.ui.DreamCueViewModelSyncTest` passed on `DreamCueTest`.
- Android account UI connected tests passed for reset-password and manual-sync entry points.
- `swiftc macos/tests/FirebaseSessionPersistenceTests.swift ... -framework Security` passed.
- `swiftc macos/tests/SyncAccountPolicyTests.swift ... -framework Security` passed.
- `xcodebuild -project macos/DreamCueMac.xcodeproj -scheme DreamCueMac -configuration Debug -destination 'platform=macOS' build` passed.
- macOS account XCUITest passed for signed-in account status, reset-password, and manual-sync entry points.
